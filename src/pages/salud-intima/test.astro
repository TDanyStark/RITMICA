---
import { URL_BASE } from "@/config";
import Layout from "@/layouts/Layout.astro";
import Footer from "@/components/Footer.astro";
import Header from "@/components/Header.astro";
import HomeSVG from "@/assets/HomeSVG.astro";
import TestSVG from "@/assets/TestSVG.astro";
import FormSaludIntima from "@/components/salud-intima/test/FormSaludIntima.astro";
---

<Layout>
  <section
    class="overflow-clip bg-position-[70%] md:bg-right bg-repeat md:bg-no-repeat md:bg-cover salud-bg"
    style={`--salud-bg: url('${URL_BASE}/img/bg_salud_intima.webp')`}
  >
    <div class="flex w-9xl max-w-full mx-auto flex-col min-h-screen pb-24">
      <Header with="tuciclo" />
      <div
        class="md:absolute right-0 md:top-16 bg-purple-abbott text-white text-center py-2 px-8 md:rounded-l-full text-sm md:text-base font-semibold"
      >
        INFECCIONES <span class="font-normal">VAGINALES</span>
      </div>
      <main
        class="flex-1 p-4 flex flex-col md:flex-row gap-2 md:gap-8 relative z-10 md:px-6"
      >
        <div class="md:border-r md:border-purple-abbott w-96 max-w-full">
          <div class="flex items-center justify-center gap-4">
            <TestSVG />
            <h1 class="text-purple-abbott text-3xl leading-5">
              <span class="block font-bold text-5xl">TEST</span>
            </h1>
          </div>
          <img
            src={`${URL_BASE}/img/womans/salud_intima_3.webp`}
            alt="Menopausia"
            class="w-full h-auto max-h-[300px] md:max-h-[600px] object-contain"
          />
        </div>
        <section class="flex-1 flex flex-col justify-between gap-8 md:px-2">
          <div
            class="contenedor_with_scroll relative flex-1 mt-6 md:max-h-[649px] overflow-auto"
          >
            <FormSaludIntima />
          </div>
          <p class="text-xs mt-4">
            <strong>Referencias: 1.</strong> Recommendations for the treatment of
            Trichomonas vaginalis, Mycoplasma genitalium, Candida albicans, bacterial
            vaginosis and human papillomavirus (anogenital warts). Geneva: World Health
            Organization; 2024. Licence: CC BY-NC-SA 3.0 IGO. <strong>2.</strong
            >
            Sobel J. Common clinical findings of vaginitis. UpToDate®. 2025 <strong
              >3.</strong
            >
            Qi W, Li H, Wang C, et al. Recent Advances in Presentation, Diagnosis
            and Treatment for Mixed Vaginitis. Front Cell Infect Microbiol. 2021;11:759795.
          </p>
          <div class="flex justify-end">
            <a href={`${URL_BASE}/salud-intima`} aria-label="Ir a Menopausia">
              <HomeSVG />
            </a>
          </div>
        </section>
      </main>
      <Footer />
    </div>
  </section>
</Layout>

<style>
  .contenedor_with_scroll::-webkit-scrollbar {
    width: 8px;
  }

  .contenedor_with_scroll::-webkit-scrollbar-track {
    background: #f1f1f1;
    border-radius: 4px;
  }

  .contenedor_with_scroll::-webkit-scrollbar-thumb {
    background: var(--color-purple-abbott);
    border-radius: 4px;
  }

  /* Salud section responsive background: pink on mobile, image on desktop */
  .salud-bg {
    background: var(--color-pink-abbott);
  }

  @media (min-width: 768px) {
    .salud-bg {
      background-image: var(--salud-bg);
      background-repeat: no-repeat;
      background-size: cover;
      background-position: right;
      background-color: transparent;
    }
  }
</style>
